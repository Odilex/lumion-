specVersion: "v1beta"
serviceId: "lumion"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lumion-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
